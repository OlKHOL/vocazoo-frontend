import CurrentRank from "../components/CurrentRank";

const HomePage = () => {
  return (
    <Container maxWidth="md" sx={{ py: 4 }}>
      <CurrentRank />
      {/* ... existing content ... */}
    </Container>
  );
};

export default HomePage;
